// Hardcoded for V1 — single-restaurant mode
export const RESTAURANT_ID = 'restaurant_001';

// Base URL for QR codes — change to production domain when deploying
export const APP_BASE_URL = window.location.origin;
